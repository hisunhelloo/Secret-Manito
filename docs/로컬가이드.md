# 💻 로컬 개발 가이드 (Local Development)

내 컴퓨터(Local)에서 직접 프로젝트를 실행하고 수정하는 방법입니다.

---

## 1. 사전 준비
이 프로젝트는 **Node.js** 환경에서 실행됩니다.
- [Node.js 설치하기](https://nodejs.org/) (LTS 버전 권장)

## 2. 프로젝트 설치
터미널(Terminal)을 열고 프로젝트 폴더에서 아래 명령어를 차례로 입력하세요.

```bash
# 1. 의존성 라이브러리 설치
npm install
```

## 3. 환경 설정
Firebase 연동을 위한 설정이 필요합니다.
`src/config/firebase.ts` 파일을 열어 `firebaseConfig` 객체에 본인의 키 값을 입력해주세요.
(Firebase 콘솔 > 프로젝트 설정 > 내 앱 > SDK 설정 및 구성에서 확인 가능)

```typescript
const firebaseConfig = {
  apiKey: "AIzaWy...",
  authDomain: "...",
  // ...
};
```

## 4. 실행 (Development Server)
개발 모드로 서버를 실행합니다.

```bash
npm run dev
```
실행 후 터미널에 표시되는 주소(예: `http://localhost:5173`)로 접속하면 웹사이트가 열립니다.

## 5. 주요 폴더 구조
- `src/components`: 화면별 UI 코드가 들어있습니다. (`Landing`, `RoomAdmin`, `ResultView`)
- `src/config`: Firebase 설정 파일이 있습니다.
- `src/index.css`: Tailwind CSS 설정과 커스텀 애니메이션이 있습니다.
